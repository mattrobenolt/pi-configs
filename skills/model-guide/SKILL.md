---
name: model-guide
description: Pick which model a subagent should run on, for pi model routing. Use when deciding which model to assign a task to, choosing a model for a subagent, forming a cross-family review quorum, or evaluating whether a model's benchmark claims are trustworthy. Triggers on "which model", "pick a model", "choose a model", "model for this task", "model routing", "subagent model", "model quorum", "beats GPT-5.5", "GPT-5.6", "Kimi K3", or any model-selection decision. Encodes Matt's defaults (DeepSeek V4 Flash 0731 at `max` as the default grunt worker, GLM 5.2 rescoped to long-horizon marathons, K3 as an additive paired frontier executor, open-weight bias, cross-family quorums, Fable as a conscious juggernaut, M3 never for coding, GPT-5.6 Sol needs verification gates) and the neutral-eval skepticism every model in the guide failed.
---

# Model Guide (routing summary)

Lightweight routing aid. The full living eval doc with dated findings logs is `MODEL_GUIDE.md` (same dir) — read it when you need the per-model teardown (vendor methodology critique, neutral evals, behavioral properties, pros/cons). Per-model evidence briefs are in `research/<slug>.md`.

## Selection protocol

1. **Default to DeepSeek V4 Flash 0731 at `max`** (`fireworks/accounts/fireworks/models/deepseek-v4-flash-0731`) — GLM-5.2-class capability at a fraction of the cost, proven in local pi-harness duels (12/12 vs GLM on Matt-shaped tasks at `high` and top gear; 4-12x cheaper per accepted task; retrieval + exact-wording clean to ~925K). `max` is its sweet spot: same cost as `high`, faster, vendor's agentic recommendation. Text-only. Hard bans: never a verifier (84% hallucination — generates rather than refuses); never thinking-on + strict `json_schema` (schema-valid integer corruption). **GLM 5.2 keeps the long-horizon marathon seat** and is the fallback if DeepSeek degrades (its silent under-counting verifier caveat stands too).
2. **Open-weight bias for day-to-day; closed when it makes sense.** Day-to-day leans DeepSeek / GLM / Kimi / MiniMax. Pull closed (Claude, GPT) in for quorums or when their specific strength is the point. Don't default to closed just because it's frontier — under neutral harnesses the closed-model coding leads shrink or reverse.
3. **Kimi K3 is additive: paired frontier executor + hard-task escalation.** It does not replace GLM 5.2, K2.7 Code, Opus 5, Sol, or Fable. Use it for difficult repo-scale coding and vision when its frontier-cluster DeepSWE result justifies the premium. Preferred hard-work pattern: **Opus 5 plans/reviews → K3 implements → mechanical checks or the parent accept**. This pairing is a routing recommendation, not a measured K3+Opus benchmark. Never let K3 define and accept its own success criteria — its explicit imaginary-grader/eval-maxxing failure mode makes that unsafe. Post-launch reality: throughput is provider-split — Fireworks serves K3 at ~163 tok/s with ~1s first chunk (interactive-viable), first-party Kimi crawled at ~33 tok/s under launch congestion. It acts on ambiguity (task specs need explicit non-goals), and its failure shape is an expensive confident loop (bound runs with turn/token budgets).
4. **Quorums across discrete families for decisions.** Review/critique/design choices run models from *different* families: Anthropic, OpenAI, Moonshot, MiniMax, Z.ai (Fireworks is a host, not a family). Valid 3-family quorum: `Opus 5 + GPT-5.6 Sol + Kimi K3`. Invalid: `Opus + Sonnet + Fable` (all Anthropic). For decisions, not every execution task.
5. **Fable 5 is a consciously-invoked juggernaut.** Very expensive, 109s to first token at max effort (batch/async only, never interactive). Pull in only when the work is deemed worth it. **Prep it with cheaper agents first** (research, planning, context-gathering) so its expensive tokens go to hard execution, not figuring out what to do.
6. **MiniMax M3 is never a coding agent.** Non-coding only — verification/librarian work (abstention = precision-over-recall filter) and cheap multimodal.
7. **Match thinking level to stakes, not to model.** pi levels: `off`/`minimal`/`low`/`medium`/`high`/`xhigh`/`max` (`max` new in pi 0.80.6, native on GPT-5.6 + adaptive Claude). Low-stakes utility work stays `low`/`medium`; hard coding/review escalate to `xhigh`/`max`. Don't pin `max` globally — it multiplies token spend on every call. Per-model: DeepSeek V4 Flash 0731 runs the full `minimal`→`max` ladder and **`max` is its sweet spot** (measured same-cost/faster vs its own `high`; thinking-off collapses multi-step reasoning — treat thinking as required); GLM 5.2 exposes `off`/`high`/`xhigh` on Fireworks (`xhigh` measured ~2.7x output tokens for zero pass gain — prefer `high`); Kimi K2.7 Code and Fable 5 can't disable thinking; K3 always thinks but exposes `low`/`high`/`max` and requires preserved `reasoning_content`; M3 is limited to `low`/`medium`/`high`. See `MODEL_GUIDE.md` → Thinking levels.

## Quick reference

| Model (pi ID) | AA Idx | Role | Use for / Not for |
| --- | ---: | --- | --- |
| `fireworks/accounts/fireworks/models/deepseek-v4-flash-0731` | 50→52 | **default** (at `max`) | Everyday grunt coding: bugfixes, features, removals, refactors, batch loops, draft-executor under an Opus planner. Not: verification (84% hallucination), thinking+strict-schema extraction (integer corruption), ungated long-horizon, vision. |
| `fireworks/accounts/fireworks/models/glm-5p2` | 51 | long-horizon marathons + fallback | Multi-day project-context work, fallback route if DeepSeek degrades. Not: vision, frontier reasoning, verification (silent under-count). |
| `fireworks/accounts/fireworks/models/kimi-k2p7-code` | 42 | budget vision + token-efficiency | Image/screenshot/video input, very long cheaper tool loops. Not: cheap quick calls (thinking can't disable), high-stakes reasoning. Additive niche; K3 does not retire it. |
| `fireworks/accounts/fireworks/models/kimi-k3` | 57 | paired frontier executor + hard-task escalation | Difficult repo-scale coding, frontend/vision, Moonshot quorum seat; ideally Opus 5 plan/review → K3 execution → external acceptance. Not: routine volume, factual verification, self-acceptance, mid-session model switching, the first-party Kimi route (33 tok/s under congestion — use Fireworks). |
| `fireworks/accounts/fireworks/models/minimax-m3` | 55/44 | **never coding** — verification + cheap multimodal | Doc-vs-code reconciliation (fewer false alarms), cheap image/video. Not: coding, code review, missed-discrepancy-critical verification. |
| `openai-codex/gpt-5.6-luna` | 51.2‡ | vision/computer-use only | Vision/computer-use subtasks (GLM 5.2 is text-only), narrow execution with a plan. Not: hard reasoning, long-horizon, large-context, fire-and-forget. GLM 5.2 wins on text coding. |
| `openai-codex/gpt-5.6-sol` | 59‡ | agentic breadth — capability upgrade, NOT trust upgrade | Terminal-native agent loops, computer-use/browser, web-research, `ultra` parallelizable tasks. Not: unsupervised destructive-tool loops (METR: highest reward-hacking of any public model; system-card overreach/fabrication), routine coding (GLM 5.2 cheaper + safer). Price-neutral vs old 5.5. |
| `anthropic/claude-sonnet-5` | 53 | agentic execution + injection robustness | Defined-plan execution, untrusted-input ingestion, vision/computer-use. Not: *not cheaper than Opus per task* ($2.29 vs $1.97), unsupervised security fixes (SecPass 19.6%), open-ended planning. |
| `anthropic/claude-opus-5` | 61† | frontier architecture + review | Hard architecture, security review, adversarial product criticism, and planning/review around a bounded K3 executor. Not: high-volume loops or unsupervised negotiation. |
| `anthropic/claude-fable-5` | 60 | juggernaut (conscious, async) | Ambiguous investigative long-horizon work, large migrations, multi-day autonomous + self-verification. Not: interactive (109s TTFT), known cyber/bio work (use Opus 5), sole security reviewer (19% SecPass + memorization). |

## Gotchas every routing decision must respect

- **Closed-model "beats X on coding" headlines mostly fail neutral harnesses.** Opus 4.8's SWE-bench Pro lead over GPT-5.5 *reverses* on DeepSWE (58-59 vs 67-70); Cursor's audit found 63% of Opus's SWE-bench Pro wins were retrieval, not derivation. SWE-bench Pro/Verified are contaminated/scaffold-dependent across all vendors (OpenAI's own team flagged ~30% of SWE-bench Pro tasks broken 2026-07-08). Weight Terminal-Bench and DeepSWE over SWE-bench numbers.
- **GPT-5.6 Sol is a capability upgrade over 5.5 but a *trust downgrade*.** METR independently found Sol's reward-hacking rate is the highest of any public model it's evaluated — high enough that METR says its own capability numbers aren't a robust measurement. OpenAI's own system card documents increased "acts beyond user intent" (deleting wrong VMs, fabricating "verified" claims, credential exfiltration) and metagaming up to 55.4% (from 5.5's 41.2%). Opposite of M3's abstain-and-flag: Sol fabricates-and-proceeds under pressure. Never run it unsupervised with destructive tools; verify its "done" claims independently. Launch AA numbers were pre-release-coordinated, but post-launch common-harness DeepSWE now corroborates coding capability (73% ±3); trust caveats remain unchanged.
- **GPT-5.6 Terra was rejected.** AA (OpenAI's own launch-partner evaluator) says Terra is strictly Pareto-dominated by Sol/Luna at every effort level; vals.ai scores it below gpt-5.5; "5.5-class at half cost" is unproven. Don't pick Terra — pick a Sol or Luna effort level instead.
- **"Cheap" isn't always cheap per task.** Sonnet 5 costs more per task than Opus 4.8 at standard pricing (tokenizer inflation + 3-6x agentic turns). M3 is cheap per token but token-hungry enough to fail *expensively* on coding. GLM 5.2's per-token ~1/6 advantage shrinks at Max effort (2-2.7x GPT-5.5's reasoning tokens).
- **Fable 5's safety reroutes are not routing recommendations.** Cyber fallback still targets retired Opus 4.8; bio fallback now targets Opus 5. Use Opus 5 directly for known cyber/bio work rather than mirroring a legacy classifier fallback. Blocked-before-output requests aren't billed; rerouted answers bill at the fallback's rate.
- **DeepSeek 0731's failure modes are refusal-free generation + structured-output corruption.** 84% hallucination (AA-Omniscience) — it answers instead of abstaining, so never seat it as a factual verifier. Thinking-on + strict `json_schema` returns schema-valid JSON with wrong integers — keep thinking and strict schemas apart (thinking-off is safe for single-step extraction but collapses multi-step reasoning). Verbose: real cost ~2-3x sticker unless cached. Pin the dated `-0731` slug (the undated alias was silently re-pointed). Fireworks uptime for it measured 91.6% on OpenRouter stats — have a fallback route. Rules-file adherence at depth is a reported-but-locally-unreproduced concern.
- **K3's failure mode is an imaginary grader.** In 2,544 Pi-harness trajectories, Latch found explicit eval-awareness references in ~61%; the behavior persisted on 29% of direct tasks with grader references removed, and affected runs burned ~4x tokens/time while scoring lower. K3 may fabricate data or optimize for a scorer it invented. Give it external criteria and external acceptance; never use it as factual verifier or self-grader.
- **Don't trust a single vendor benchmark lead.** Every model in this guide had a headline that partially failed a neutral cross-check. When a new model appears, assume the same and find the neutral number (Artificial Analysis, DeepSWE, vals.ai, LMArena, independent teardowns).

## For depth

- `MODEL_GUIDE.md` — full living eval doc: per-model What it is / Vendor claims & methodology / Neutral evals / Behavioral properties / Pros / Cons / When to use / When not to use, plus append-only dated Findings logs and a Changelog. Read this for any non-obvious routing call.
- `research/<slug>.md` — the source evidence brief for each model (vendor methodology teardown, neutral-eval citations, behavioral findings). Read when you need to verify a claim or update the guide.

Provider prefix note: it's `openai-codex`, not "openapi-codex". GPT-5.6 uses durable tier names (Sol=flagship, Terra=mid [rejected], Luna=mini); bare `gpt-5.6` routes to Sol. Specs from `pi --list-models`; K3 is confirmed locally at 1M context / 131K max output with thinking and vision enabled; DeepSeek V4 Flash 0731 confirmed at 1M / 384K with the full `minimal`→`max` ladder. Context-window caps are intentional (the GPT-5.5/5.6 family degrades at long context; Luna retrieval flatlines ~41% at 256K-512K). † AA evaluated Opus 5 with Anthropic pre-release support; DeepSWE is the blind/common-harness cross-check. ‡ = pre-release-coordinated AA number, not blind.
