# LSP Plugin

This is a Claude Code plugin for local language-server configuration.

The actual configuration lives inline in `.claude-plugin/plugin.json` under `lspServers`. The directory otherwise looks empty because Claude plugin metadata lives under hidden dot directories.

This is not used by pi.
